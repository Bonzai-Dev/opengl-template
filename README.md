# opengl-template
An OpenGL project template using the 4.6 core profile along with CMake and SDL3

## Cloning
```bash
git clone --recursive
```

## Building
```bash
mkdir build
cd build
cmake ..
cmake --build . -j 8
```
